# P-03 · Door QR card

## Meta

Opened from `L08-BTN-PRINTQR` (bulk, via `L08-CHK-SELECT`) and
`L09-BTN-QR` (single unit). See `cross-cutting.md` § Print-view contract
for the rules shared by all print views.

## Contents

Unit number · property name · the QR (linking to that unit's `T-01`) · one
line: "Scan to report a problem" · landlord's phone as a fallback. Sized
four to an A4 page.
